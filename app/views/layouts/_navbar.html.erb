<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand btn btn-danger" href="/">Private Events</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home</a>
      </li>
    </ul>
    <% if signed_in? %>
      <%= link_to "Sign out", signout_path, class: "btn btn-success" %>
    <% else %>
      <%= link_to "Sign up now", signup_path, class: "btn btn-info" %>
      <%= link_to "Sign in", signin_path, class: "btn btn-success" %>
    <% end %>

  </div>
</nav>